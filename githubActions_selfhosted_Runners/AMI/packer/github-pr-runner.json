{
  "variables": {
    "aws_region": "eu-central-1",
    "aws_vpc": "vpc-04d64ae6866c7d6d3",
    "aws_subnet": "subnet-0dfcf6324786fd8ff",
    "is_public": "false",
    "mysql_password": "{{ vault `/amway/data/bamboo/agents/mysql_root_password` `password` }}",
    "github_token": "{{ vault `/amway/data/github/git_admin_user_4_enterprise` `token_repo_scope` }}"
  },
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "{{user `aws_region`}}",
    "vpc_id": "{{user `aws_vpc`}}",
    "subnet_id": "{{user `aws_subnet`}}",
    "iam_instance_profile": "dev_debug",
    "source_ami_filter": {
      "filters": {
      "virtualization-type": "hvm",
      "name": "centos7-golden*",
      "root-device-type": "ebs"
      },
      "owners": ["self"],
      "most_recent": true
    },
    "instance_type": "t3.large",
    "ssh_username": "centos",
    "associate_public_ip_address": "{{user `is_public`}}",
    "ami_name": "ga-runner-pr {{ isotime `2006-01-02-03-04` }}",
    "tags": {
      "Name": "GitHub self-hosted runner for prs",
      "OwnedBy": "Vladimir Antropov",
      "ApplicationID": "APP1433689",
      "DataClassification": "Internal",
      "Environment": "DEV"
    }
  }],
  "provisioners": [
    {
      "type": "shell-local",
      "inline": "cd ../ansible/github-actions/pr-runner && ansible-galaxy install -r requirements.yml -p ../../roles/ -f"
    },
    {
      "type": "ansible",
      "extra_arguments": [
        "--extra-vars", "db_root_password={{ user `mysql_password` }} github_token={{ user `github_token` }}"
      ],
      "playbook_file": "../ansible-eu/github-actions/pr-runner/main.yaml"
    }
  ]
}
