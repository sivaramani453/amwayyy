{
  "variables": {
    "aws_vpc": "vpc-04d64ae6866c7d6d3",
    "aws_subnet": "subnet-0dfcf6324786fd8ff",
    "is_public": "false"
  },
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "{{user `aws_region`}}",
    "vpc_id": "{{user `aws_vpc`}}",
    "subnet_id": "{{user `aws_subnet`}}",
    "source_ami_filter": {
      "filters": {
      "virtualization-type": "hvm",
      "name": "centos8-golden*",
      "root-device-type": "ebs"
      },
      "owners": ["self"],
      "most_recent": true
    },
    "instance_type": "t3.large",
    "ssh_username": "centos",
    "associate_public_ip_address": "{{user `is_public`}}",
    "ami_name": "mdms_proxy {{ isotime `2006-01-02-03-04` }}",
    "tags": {
      "Name": "This image contains simple Nginx-based caching proxy, intented for returning stalled responses while upstream env is during maintenance.",
      "OwnedBy": "Jan Machalica",
      "ApplicationID": "APP1433689",
      "DataClassification": "Internal",
      "Environment": "DEV"
    }
  }],
  "provisioners": [
    {
      "type": "shell-local",
      "inline": "cd ../ansible-eu/mdms-proxy && ansible-galaxy install -r requirements.yml -p ../roles/ -f"
    },
    {
      "type": "ansible",
      "playbook_file": "../ansible-eu/mdms-proxy/main.yml"
    }
  ]
}

